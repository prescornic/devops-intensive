apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "users-stack.fullname" . }}-test-connection"
  labels:
    {{- include "users-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "users-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
