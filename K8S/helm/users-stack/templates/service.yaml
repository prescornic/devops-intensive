apiVersion: v1
kind: Service
metadata:
  name: {{ include "users-stack.fullname" . }}
  labels:
    {{- include "users-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "users-stack.selectorLabels" . | nindent 4 }}
